# RNApp
react-native code assignment

## Install dependencies
```
Clone this repo
cd RNApp
npm install -g react-native-cli
npm install
```

## Run on ios
```
cd ios && pod install
react-native run-ios
```